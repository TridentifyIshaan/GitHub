/*

> Date Created: June 14, 2023
> Author: Ishaan Rastogi
> Purpose: This code is to showcase correct arithmatic instructions
> Operating System: This is only for Windows OS, it may or may not work on other OS
> Program Status: 100% Working

*/

// NOTE: Don't run the code by removing /**/ because no value is assigned to variables even for valid ones, so it will turn out to show you error.

#include<stdio.h>
#include<math.h> // For Power Function

int main()
{
    /*
    
    Topic: Arithmatic Instructions
    a, b are operands while + is operator
    Single variables should be on LHS
    
    Valid:
    
    a = b + c;
    a = b * c;
    a = b / c;
    a = pow(b,c)

    Invalid:

    b + c = a; // As it voilates the statement stated in line 22.
    a = bc;
    a = b^c;           ^ is treated as XOR operator in C lang, to perform bitwise operation which will be told after sometime ( machine code, binary code, 0 1 1 0)
    
    */
}

// No code available to run as stated at the top